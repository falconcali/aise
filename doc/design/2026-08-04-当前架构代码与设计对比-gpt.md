# AISE 当前架构代码与设计对比

## 1. 文档定位

本文记录 AISE 当前代码与 [Architecture.md](./Architecture.md) 最终架构设计之间的对应关系和差距。

本文不是架构规范。发生冲突时，以 `Architecture.md` 为准。本文应随实现进度更新，用于回答：

- 架构设计中的职责当前由哪个类型承担。
- 哪些能力已经实现。
- 哪些能力只有骨架或部分实现。
- 哪些目标能力尚未实现。
- 当前实现是否存在违反最终架构的不安全路径。

对比基线：2026-08-03 workspace 代码。

---

## 2. 当前运行链路

```text
Story Client
      |
      v
aise-server::api::turn::run_turn
      |
      v
SessionRegistry::get
      |
      v
tokio::spawn
      |
      v
Session::lock_turn
      |
      v
AiseEngine::run_turn
      |
      v
TurnRuntime::run
      |
      v
Configured Pipeline Vec
      |
      v
TurnCommitter -> Store::commit_turn -> SqliteStore
      |
      v
TurnEventSink -> SseSink -> Story Client
```

代码中没有 `TurnAdmission` 或 `StoryTurnCoordinator` 类型。Story Turn 串行化目前由 `aise-server::session::Session::lock_turn` 承担，因此最终架构直接沿用 `Session::lock_turn`，没有重新引入同义组件。

---

## 3. 架构组件映射

| 架构职责 | 当前代码 | 状态 | 说明 |
| --- | --- | --- | --- |
| HTTP/SSE Turn 入口 | `aise-server::api::turn::run_turn`、`SseSink` | 已实现 | 查找 Session、创建 SSE channel、启动 Turn 任务 |
| Session 管理 | `SessionRegistry` | 已实现 | 内存 HashMap，受 `max_sessions` 限制 |
| 同一 Session 串行 | `Session::lock_turn` | 已实现 | 单进程异步 Mutex |
| 所有入口按 Story 串行 | 无统一强制 | 未实现 | 直接调用 `AiseEngine::run_turn` 可绕过 Session 锁 |
| Engine 生命周期 | `AiseEngine::run_turn` | 已实现 | 创建 Context、根 Trace、调用 Runtime、发布结果 |
| Pipeline 接口 | `TurnExecutionPipeline` | 已实现 | `stage()` 与 `Result<(), AiseError>` |
| Pipeline fail-fast | `TurnRuntime::run` | 已实现 | Pipeline 返回 Err 后停止后续阶段 |
| 固定工作流 | `aise-server::app::build_engine` | 部分实现 | 当前由调用方传入任意 Pipeline Vec，Runtime 未强制拓扑 |
| Turn Context | `TurnExecutionContext` | 部分实现 | 生命周期正确，但字段全部公开且没有阶段状态机 |
| Turn Budget | `TurnBudget`、`TurnConfig` | 部分实现 | 配置、Context 和实际消费没有统一 |
| Baseline Context | `BaselineContextBuilder` | 部分实现 | 只加载角色和历史 Turn |
| Writer Planning | `WriterPlanner` | 骨架 | 当前输出固定计划 |
| Context Retrieval | `ContextRetrievalPipeline` | 骨架 | 当前只复制 recent story |
| Character Think | `CharacterThinkPipeline` | 骨架 | 当前不调用 LLM |
| Story Generation | `StoryGenerator` | 部分实现 | 当前只使用 player input |
| Validation | `ValidationPipeline` | 部分实现 | Schema 检查有限，Consistency 固定通过 |
| Story Repair | `StoryRepairer` | 骨架 | 当前 no-op，未装配 |
| SQLite 原子事务 | `SqliteStore::commit_turn` | 已实现 | 单数据库事务 |
| 提交协调 | `TurnCommitter` | 部分实现 | 直接读取 StoryDraft，没有 ValidatedChangeSet |
| Story revision | 无 | 未实现 | 没有 compare-and-swap |
| 请求幂等 | 无 | 未实现 | turn_id 唯一约束不能替代完整幂等协议 |
| Transactional Outbox | 无 | 未实现 | 派生状态尚无可靠投递协议 |
| LLM 抽象 | `LlmProvider` | 已实现 | completion 与 streaming |
| LLM 共享并发限制 | `LlmLimiter`、`OpenAiCompatProvider` | 已实现 | Provider 发请求前获取 semaphore permit |
| Turn Trace | `TraceRecorder` | 已实现 | 默认最多 64 spans，记录丢弃数量 |
| Turn deadline / cancellation | 无统一协议 | 未实现 | 外部调用和后台任务未统一受控 |

---

## 4. Turn 串行化

### 4.1 已实现行为

`Session` 持有 `tokio::sync::Mutex<()>` 类型的 `turn_lock`。HTTP Turn handler 在后台任务中获取该锁，并持锁执行完整的 `AiseEngine::run_turn`。

当前 API 创建 Session 时同时生成唯一 `StoryId`，Session 与 Story 是一对一关系。因此在当前单进程 HTTP 入口中：

- 同一 Story 的 Turn 串行。
- 不同 Story 使用不同 Session 锁，可以并行。
- 全局 LLM 调用仍受共享 `LlmLimiter` 限制。

### 4.2 与设计的差距

- `AiseEngine::run_turn` 是公开方法，核心库调用方可以绕过 `Session::lock_turn`。
- 串行化能力位于 HTTP Session 层，没有覆盖未来 CLI 或消息消费者入口。
- 多实例部署之间没有共享锁、租约或 fencing token。
- Mutex 等待队列没有显式容量、超时和拒绝策略。
- Session 删除与正在执行的 Turn 没有统一生命周期协议。

---

## 5. 固定工作流与 Runtime

`aise-server::app::build_engine` 和集成测试当前使用相同顺序：

```text
TurnInitializer
    -> BaselineContextBuilder
    -> WriterPlanner
    -> ContextRetrievalPipeline
    -> CharacterThinkPipeline
    -> StoryGenerator
    -> ValidationPipeline
    -> TurnCommitter
```

当前 `TurnRuntime::new` 接受 `Vec<Box<dyn TurnExecutionPipeline>>`，因此调用方仍可重排、遗漏或增加 Pipeline。固定拓扑目前是 composition root 的约定，不是 Runtime 强制的不变量。

`ContextRetrievalPipeline` 和 `CharacterThinkPipeline` 总会被 Runtime 调用，再根据 `WriterPlan` 自行 no-op。最终设计要求由 Runtime 控制条件阶段。

`StoryRepairer` 已有类型，但没有加入 Pipeline Vec；Runtime 也没有 Validation / Repair 循环。

---

## 6. Pipeline 返回值与错误

当前接口与设计一致：

```rust
#[async_trait]
pub trait TurnExecutionPipeline: Send + Sync {
    fn stage(&self) -> &'static str;

    async fn execute(
        &self,
        ctx: &mut TurnExecutionContext,
    ) -> Result<(), AiseError>;
}
```

`TurnRuntime::run` 会记录 `StageStarted` 和 Pipeline Trace，根据 `Ok` / `Err` 写入 `PipelineData`，随后通过 `outcome?` fail-fast。

当前 `AiseError` 包含 StoryNotFound、LLM、Store、Migration、I/O、JSON、ValidationBudgetExhausted 和 Internal。设计要求的 Validation 不可修复、Turn 超时、取消、revision 冲突及幂等冲突尚未形成独立错误类型。

---

## 7. TurnExecutionContext 与预算

当前 Context 字段：

```text
story_id / turn_id / player_input
baseline_ctx / plan / retrieved_ctx / character_thoughts
draft / validation / budget / trace
```

主要差距：

- 所有字段均为 `pub`，Pipeline 可以修改任意区域。
- 没有 `TurnPhase`、`StorySnapshot`、`base_revision` 或 `ValidatedChangeSet`。
- `TurnExecutionContext::new` 先创建空 `turn_id`，之后由 Initializer 填充，存在半初始化状态。
- 阶段正确性依赖 `Option` 和调用顺序，没有显式前后置条件。

当前 `TurnBudget` 定义：

- `max_repair_rounds = 3`。
- `max_tokens = 2048`。
- `max_retrieved_items = 20`。

`max_retrieved_items` 已用于历史加载和 Retrieval。Story Generator 使用构造时传入的 `TurnConfig.max_tokens`，不消费 `ctx.budget.max_tokens`。`max_repair_rounds` 尚未接入控制流。当前没有总 token 计数、deadline、单次 timeout 或取消信号。

---

## 8. Context、Planning、Character 与 Story

### 8.1 Baseline Context

`BaselineContextBuilder` 当前调用：

- `Store::load_characters`。
- `Store::load_story`。

它填充 player character、relevant characters 和 recent story。World、Memory、Story Instructions、Current Scene、Summary 和 Constraints 尚未加载，也没有统一 revision snapshot。

### 8.2 Writer Planner

当前 Planner 不调用 LLM。它把 `player_input` 复制到 `StoryGoal.summary`，并固定关闭 Retrieval 与 Character Think。`need_*` 布尔值和请求集合同时存在，可能产生互相矛盾的计划状态。

### 8.3 Context Retrieval

当前 Retrieval 只把 recent story 转换为 `HistoricalStory` 类型的 `ContextItem`。尚未接入 Character Memory、World Knowledge、Narrative Graph 或 Lore Book，也没有 Context Merger、来源权限和 token 合并策略。

### 8.4 Character Think

当前 Character Think 不调用 LLM。需要执行时只按请求角色生成占位 Thought，并把 player input 复制到 perception。

### 8.5 Story Generator

当前 Generator 只把 player input 作为单条 user message 发送给 LLM，没有组合 Baseline Context、WriterPlan、Retrieved Context 或 Character Thoughts。

Generator 生成 Story Text 和一个 Action Event；Character、World、Memory updates 为空。调用为非流式 completion。

---

## 9. Validation 与 Repair

当前 `ValidationPipeline` 依次运行：

- `SchemaValidator`：仅检查 Draft 是否存在以及 Story Text 是否为空。
- `ConsistencyValidator`：当前固定返回通过。

`ValidationIssue` 当前具有 severity、code 和 message，尚无 repairability 和 location。

关键差距：Validation 不通过时 Pipeline 仍返回 `Ok(())`，Runtime 不检查 `ctx.validation.pass`，因此 Committer 仍会执行。这违反“未通过验证不得提交”的架构不变量。

`StoryRepairer` 当前为 no-op，没有装配。`max_repair_rounds` 和 `ValidationBudgetExhausted` 已定义但未接入。

---

## 10. 提交与持久化

### 10.1 已实现行为

`TurnCommitter` 持有 `Arc<dyn Store>`，从 `ctx.draft` 构造 `TurnCommit`。`SqliteStore::commit_turn` 使用一个 SQL transaction 写入：

- World。
- Story Turn。
- Story Events。
- Characters。
- Memory。

单数据库事务原子性已经具备。

### 10.2 与设计的差距

- Committer 直接接受未转换的 StoryDraft，没有 `ValidatedChangeSet`。
- 当前只实际提交 Story Turn 和 Events；Character、World、Memory 与 Summary 输入为空。
- 没有 Story revision 和 compare-and-swap。
- 没有请求幂等键和返回原 Turn 的恢复协议。
- 没有 Outbox 和派生状态消费者。
- 没有明确 Commit marker 或 Turn 恢复状态。

当前还有一个需要优先修正的语义问题：`TurnCommit.world == None` 会被 `SqliteStore` 转换为空 `WorldState` 后执行 upsert，而不是“不修改 World”，可能覆盖已有 World 内容。

---

## 11. 结果、SSE 与任务生命周期

`AiseEngine::run_turn` 在 Runtime 成功后发出：

```text
TurnEvent::Validation
TurnEvent::Token
TurnEvent::Finished
TurnEvent::Trace
```

`Token` 当前是完整 Story Text，不是 LLM delta。由于 Committer 是末尾 Pipeline，成功事件在提交完成后发出，这一点符合设计。

差距：

- Runtime 失败时只产生 Trace，HTTP 后台任务记录错误；SSE 客户端没有显式失败终态。
- `TurnResult` 只有 turn_id 和 story_text，没有 committed revision。
- `SseSink` 使用 unbounded channel，没有背压。
- SSE 发送失败和 Trace JSON 序列化失败被忽略。
- `tokio::spawn` 的 Turn 任务没有明确 owner、取消或 shutdown 等待协议。

---

## 12. LLM、Trace 与容量

### 12.1 LLM

应用组合根创建共享 `LlmLimiter` 并注入 `OpenAiCompatProvider`。Provider 的 completion 和 streaming 都会先获取 semaphore permit，符合共享并发限制设计。

尚缺：

- limiter 等待 timeout。
- Turn deadline 和取消传播。
- embedding 接口及其并发限制。
- token 实际消费统计。

### 12.2 Trace

`TraceRecorder` 默认最多保存 64 spans，超出后记录 `dropped_span_count`。当前已记录 Turn、Pipeline、LLM Call、Store Tool Call、Validation 和 Persist。

Prompt 与 Response 分别限制为 2000 和 4000 字符。仍需补充隐私分类和敏感内容清理策略。

### 12.3 已有容量边界

- `SessionRegistry.capacity`。
- `TurnBudget.max_retrieved_items`。
- `LlmLimiter` semaphore。
- Trace span 数量限制。
- LLM Trace 文本截断。

尚未受控的主要资源包括 HTTP Turn task、SSE channel、角色/世界集合、Validation Issues、持久化历史和派生任务。

---

## 13. 优先差距

### P0

1. Validation 失败不会阻止 Commit。
2. `TurnCommit.world == None` 可能以空 World 覆盖已有状态。
3. 直接调用 `AiseEngine::run_turn` 可以绕过同 Story 串行化。
4. Repair Loop 未实现且修复预算未消费。

### P1

1. Runtime 未强制固定工作流拓扑。
2. Context 缺少私有字段、阶段状态和写权限边界。
3. 缺少 Story revision、幂等提交和崩溃恢复协议。
4. 缺少 `ValidatedChangeSet` 领域授权边界。
5. Turn 任务与 SSE channel 缺少背压、取消和 shutdown 协议。
6. Budget 配置、Context 数据和实际消费没有统一。
7. Context、Planner、Character Think、Validation 仍主要是骨架实现。

---

## 14. 维护规则

- `Architecture.md` 只描述最终架构，不记录实现进度。
- 本文只记录实现映射和差距，不改变架构决策。
- 新增与设计同义的组件前，先确认现有类型是否已经拥有该职责。
- 代码实现某项差距后，应更新本文对应状态和说明。
- 如果实现需要偏离 `Architecture.md`，必须先完成架构评审并更新设计文档，不能只修改本文来迁就代码。
